# def sigmoid(x):
#     if type(x) == Tensor:
#         return tensor_ops.sigmoid(x)
#     else:
#         x = ensure_array(x)
#         return array_ops.sigmoid(x)

# def tanh(x):
#     if type(x) == Tensor:
#         return tensor_ops.tanh(x)
#     else:
#         x = ensure_array(x)
#         return array_ops.tanh(x)

# def relu(x):
#     if type(x) == Tensor:
#         return tensor_ops.relu(x)
#     else:
#         x = ensure_array(x)
#         return array_ops.relu(x)

# def softplus(x):
#     if type(x) == Tensor:
#         return tensor_ops.softplus(x)
#     else:
#         x = ensure_array(x)
#         return array_ops.softplus(x)

# __all__ = ["sigmoid", "tanh", "relu", "softplus"]
